const Card = ({title, description}) => {
    return (
        <div className="card">
            Your title is {title} and the description is {description}
        </div>
    )
}

export default Card;